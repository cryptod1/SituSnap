name: SituSnap APK Build
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Create Android project
      run: python3 .github/scripts/make_android.py
    - name: Build APK
      run: gradle assembleDebug
